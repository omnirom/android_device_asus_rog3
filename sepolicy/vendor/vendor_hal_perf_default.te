allow vendor_hal_perf_default vendor_sysfs_kgsl_gpuclk:file r_file_perms;

# Display
get_prop(vendor_hal_perf_default, vendor_dfps_prop)

# Dongle
get_prop(vendor_hal_perf_default, asus_dongle_prop)

# GameCenter
get_prop(vendor_hal_perf_default, asus_rog_prop)
set_prop(vendor_hal_perf_default, asus_rog_prop)